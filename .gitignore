# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.DS_Store

# Secrets. The refresh token from bootstrap.py belongs in CI secrets,
# never in the repo.
.env*
*.env

# NOTE: log/ is deliberately NOT ignored. The NDJSON history is the
# project's only state -- there is no database -- and it is committed
# back by the weekly job.
